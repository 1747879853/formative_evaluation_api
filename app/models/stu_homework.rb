class StuHomework < ApplicationRecord
 #  def as_json(options = {})
	# h = super(options)	
	# h[:title] = self.name
	# h[:checked] = false
	# h[:checked_id] = evaluations.map(&:id)
	# h
 #  end

 
 #status 0为暂存  1为提交
end